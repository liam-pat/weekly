services:
  weekly:
    image: node:20
    container_name: weekly
    labels:
      - dev.orbstack.domains=weekly.orb.local
    volumes:
      - "./:/weekly"
    working_dir: "/weekly"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - 4321:4321